---
- name: Update initramfs
  ansible.builtin.command:
    cmd: update-initramfs -u -k all
  changed_when: true

- name: Update grub
  ansible.builtin.command:
    cmd: update-grub
  changed_when: true
