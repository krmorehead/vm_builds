---
- name: Build WireGuard image
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Build WireGuard image if not cached
      ansible.builtin.command:
        cmd: >-
          {{ playbook_dir }}/../../scripts/build-images.sh
          --host {{ lookup('env', 'PRIMARY_HOST') }}
          --only wireguard
      register: _build
      changed_when: "'already exists' not in _build.stdout"
